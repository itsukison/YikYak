Critical bugs

Critical bugs
The app's name is shown as createmobile app instead of Hearsay when downloaded. It uses weird image instead of the app icon when first downloaded and loading
The little language button on top right when user onboard is not working, making users exit the app
When user creates an account in auth page, even when they entered two passwords that don't match, the account gets created. As a result, users are stuck in the sign up page since it says the account already exists even though they couldn't proceed to onboard page
The onboard page keeps on flickering and doesn't work, unless user refreshes the page and comes back to the app
The onboard page should have better UX composed of multiple pages asking for language preference, location, etc.
The upvote downvote button is not working. It doesn't get updated on the UI immediately, user has to switch to other tabs and come back to see it takes effect
The filtering for the popular tab is not working ideally. Sometimes user see posts from last week in the day tab, or posts from last months in week tab. It doesn't update correctly when user switch around the day week month tabs
The database and policies might be too messy.
The comment section doesn't work, loading forever

UI bugs
We should use skeleton loading for loading states instead of the loading spinner
For some pages the go back button on the top left slides the page to the left yet that is so unnatural so we should slide it to the right
The first onboard page uses different font from the rest. Overall the app should look more minimalist and consistent with similar corner rounding and font sizes and so on. Refer to the UI but not color of the images in @inspo
Change the nav bar to look like those in the images too